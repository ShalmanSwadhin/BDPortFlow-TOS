[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short
markers =
    smoke: smoke tests for critical functionality
    regression: full regression coverage
    api: API integration tests (requires backend on port 5000)
    ui: Selenium UI tests (requires frontend on 5173 and backend on 5000)
